#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main () {
    double num = 4.5;

    // int ans = ceil(num);
    // int ans = floor(num);
    // int ans = round(num);
    // int ans = sqrt(num);
    // int ans = pow(3, 2);
    int ans = abs(-3);

    printf("%d\n", ans);
    
    return 0;
}